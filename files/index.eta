---
layout: base.eta
title: Files
---

<section class="hero">
  <p class="eyebrow">Day to day documents, images</p>
  <h1>Files</h1>
</section>

<ul class="post-list">
  <% const files = (it.posts ?? (it.site && it.site.data && it.site.data.posts) ?? [])
    .filter((item) => item.type === "file")
    .slice()
    .sort((a, b) => (new Date(b.date).getTime() || 0) - (new Date(a.date).getTime() || 0));
  %>
  <% files.forEach((post) => { %>
    <li>
      <a href="<%= post.url %>">
        <div class="post-title"><%= post.title %></div>
      </a>
      <div class="post-meta">
        <% if (post.date) { %><span><%= new Date(post.date).toDateString() %></span><% } %>
      </div>
    </li>
  <% }); %>
</ul>
